name: ghbot
version: 1.0.0
description: GitHub Plugin for PolymorphicBot
dependencies:
  github: ">=2.1.6 <3.0.0"
  http: '>=0.11.1+1 <0.12.0'
  irc: '>=2.0.0 <3.0.0'
  polymorphic_bot:
    git: git://github.com/PolymorphicBot/PolymorphicBot.git
plugin:
  dependencies:
  - google
  commands:
    "gh-stars":
      description: "Gets the Amount of Stars a repository has"
      usage: "gh-stars [user] <repository>"
